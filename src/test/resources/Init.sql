create table t_o
(
    id   varchar(10) primary key,
    name varchar(10)
);

insert into t_o(id, name)
values ('1', '2'),
       ('2', '2');

create table t_bean(
id varchar(10) primary key,
desc varchar(10)
);

insert into t_bean(id, desc)
values ('1', '2'),
       ('2', '2');
